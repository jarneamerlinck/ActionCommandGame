@using ActionCommandGame.Ui.WebApp.Views.Home
@model IList<ActionCommandGame.Services.Model.Results.ItemResult>
@{
    ViewData["Title"] = "Shop";
    /*
    var shopList = new List<IShopItem>
    {
        new ShopItem{Id=0, Name = "PickAxe 1",
                        Price = 16, Description ="Beter then starter pickaxe", 
                        ImageLocation = "https://freepngimg.com/thumb/microphone/11-microphone-png-image-thumb.png",
                        StatsCategory = "Attack", StatsValue = 3
        },
        new ShopItem{Id=1, Name = "Chicken panini",
                        Price = 3, Description ="Fooooooood",
            StatsCategory = "Fuel", StatsValue = 20},
        new ShopItem{Id=2, Name = "Golden dress", 
                        Price = 150, Description ="Why even",
            StatsCategory = "Defence", StatsValue = 15},
        new ShopItem{Id=3, Name = "Other Item", 
                        Price = 34}
    };*/

}
@if (User.Identity is not null && User.Identity.IsAuthenticated)
{
    <a type="button" class="btn redTextWithHover" asp-controller="Account" asp-action="Logout" asp-route-id="">Logout</a>
}	
else
{
    <a type="button" class="btn redTextWithHover" asp-controller="Account" asp-action="Login" asp-route-id="">Login</a>
}
<div class="container bg-gradient rounded">
    <div class="row">
        @foreach(var shopListItem in @Model)
        {
            <partial name="ShopPartial" model="shopListItem"/>
        }
    </div>
</div>